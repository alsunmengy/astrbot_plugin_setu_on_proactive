# 更新日志

## [1.4.0] - 2026-08-12
- **修复 setu 图插进主动消息分段中间的问题**：分段/TTS 每段发送都会触发钩子并**重置**该会话的延迟任务，只有最后一段触发的任务能存活
- `delay_before_execute` 语义变更：主动消息**最后一段发出后** N 秒再投递指令，图永远出现在全部文本段落之后
- 去掉手动去重窗口，由"每会话单任务重置"机制天然保证一轮只投递一次（`dedup_window_seconds` 保留兼容）

## [1.3.0] - 2026-08-11
- 新增 `delay_before_execute` 配置：主动消息触发后延迟 N 秒再执行指令（钩子立即返回，延迟在后台任务执行，不阻塞主动消息发送）
- 新增 `command_interval` 配置：多条指令之间按 N 秒间隔逐条执行，避免瞬间连发被限流
- 去重窗口自动覆盖延迟执行周期，防止延迟期间重复触发

## [1.2.2] - 2026-08-11
- `support_platforms` 扩展为多平台：aiocqhttp (OneBot v11)、qq_official (QQ官方机器人平台)、telegram、wecom (企业微信应用)、lark (飞书)、dingtalk (钉钉)、kook

## [1.2.1] - 2026-08-09
- 补齐市场元数据字段：`repo`、`support_platforms: [lark]`、`short_desc`、`tags`、`social_link`

## [1.2.0] - 2026-08-09
- 声明 AstrBot 版本要求：`astrbot_version: >=4.0`，不满足版本的 AstrBot 将提示不支持
- 版本号规范化为 semver 三段式

## [1.1.1] - 2026-08-09
- `display_name` 改为中文名「主动消息联动」，方便在插件市场搜索

## [1.1.0] - 2026-08-09
- 新增 `commands` 配置项：支持配置多条指令，主动消息触发后逐条执行
- 兼容旧版单条 `command` 配置，旧配置无需改动

## [1.0.0] - 2026-08-08
- 初始版本：主动消息插件发送消息时，自动向同一会话投递 `/setu` 命令（仅私聊）
- 同一会话 60 秒去重窗口，防止分段发送重复触发
